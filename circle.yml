machine:
  node:
    version: 4.3.2
  pre:
    - curl -o- -L https://yarnpkg.com/install.sh | bash

dependencies:
  cache_directories:
    - node_modules
  override:
    - yarn

deployment:
  production:
    branch: master
    commands:
      - yarn global add retry-cli@0.4.0
