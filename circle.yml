machine:
  node:
    version: 4.3.2

dependencies:
  cache_directories:
    - node_modules
  override:
    - npm install -g yarn
    - yarn

deployment:
  production:
    branch: master
    commands:
      - yarn install -g retry-cli@0.4.0
      - yarn run deploy
